@model Core.Entities.ProfileViewModel
@{
    ViewData["Title"] = "Profile";
    Layout = "_PatientLayout";
}

<div class="container my-5">
    <h2 class="text-center mb-4 display-4 fw-bold text-light">Your Profile</h2>
    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success shadow" role="alert">
            @TempData["Message"]
        </div>
    }
    <div class="card shadow glassmorphic bg-dark text-light mb-5">
        <div class="card-body p-4">
            <form asp-action="UpdateProfile" method="post">
                <div class="mb-3">
                    <label class="form-label fw-semibold">Full Name</label>
                    <input asp-for="Name" class="form-control bg-dark text-light border-0" />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Email</label>
                    <input asp-for="Email" class="form-control bg-dark text-light border-0" />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Disease</label>
                    <input asp-for="Disease" class="form-control bg-dark text-light border-0" />
                </div>
                <hr class="border-light" />
                <h5 class="fw-semibold mb-3">Change Password</h5>
                <div class="mb-3">
                    <label class="form-label fw-semibold">New Password</label>
                    <input asp-for="NewPassword" class="form-control bg-dark text-light border-0" type="password" placeholder="Enter new password" />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Confirm New Password</label>
                    <input asp-for="ConfirmPassword" class="form-control bg-dark text-light border-0" type="password" placeholder="Confirm new password" />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Old Password</label>
                    <input asp-for="OldPassword" class="form-control bg-dark text-light border-0" type="password" placeholder="Old password" />
                </div>
                <button type="submit" class="btn btn-primary w-100 mt-3">Update Profile</button>
            </form>
        </div>
    </div>

    @if (Model.Appointments != null)
    {
        <h3 class="mb-4 fw-bold text-light">Your Appointments</h3>
        <div class="row row-cols-1 row-cols-md-2 g-4">
            @foreach (var appointment in Model.Appointments)
            {
                <div class="col">
                    <div class="card h-100 appointment-card text-light border-0 shadow-lg">
                        <div class="card-body">
                            <h5 class="card-title fw-bold">@appointment.DoctorName</h5>
                            <p class="card-text"><strong>Specialization:</strong> @appointment.Doctor_Specialization</p>
                            <p class="card-text"><strong>Date:</strong> @appointment.AppointmentDate.ToString("yyyy-MM-dd HH:mm")</p>
                            <p class="card-text"><strong>Description:</strong> @appointment.Description</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <h3 class="mb-4 fw-bold text-light">No Appointments Found</h3>
    }
</div>

<style>
    body {
        background: linear-gradient(135deg, #1f1c2c, #121212);
        color: #f5f5f5;
    }
    h2, h3 {
        color: #ffffff;
    }

    label {
        color: #ddd;
    }
    input {
		background-color: #1f1c2c;
		border-color: #ddd;
		color: #ddd;
	}
    .btn-primary {
        background-color: #ff0000;
        border-color: #ff0000;
    }

        .btn-primary:hover {
            background-color: #cc0000;
            border-color: #cc0000;
        }
</style>


